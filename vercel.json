{
    "buildCommand": "cd apps/temples-web && npm install && npm run build",
    "outputDirectory": "apps/temples-web/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}